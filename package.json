{
  "name": "work-log-stats",
  "version": "1.0.0",
  "description": "工作日志统计系统",
  "main": "index.html",
  "scripts": {
    "build": "if not exist dist mkdir dist && copy index.html dist && copy style.css dist && copy app.js dist && copy chart.js dist"
  },
  "keywords": ["工作日志", "统计", "前端"],
  "author": "",
  "license": "MIT"
}